

// multiple backup node possible
// multiple script possible
// multiple vip possible for multiple senario like s3 RAS
// cluster infrastructure such as shared disk file systems, in order to protect processes from other active nodes modifying the resources during node failures. 
// A notify script can be used to take actions, not only removing or adding an IP to an interface. It can for example start or stop a daemon, depending on the VRRP state.
// track_script will run after every specific intervel. This script needs to have a return value: 0 for "everything is fine", 1 (or other than 0) for "something went wrong".

//The systems need to be in the same Broadcast domain
// It is deprecated as corosync pashemaker



